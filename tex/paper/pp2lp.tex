% ════════════════════════════════════════════════════════════════════
%  Independently Checking Atelier B's Predicate Prover in LambdaPi
%  LPAR-26 (Experimental & Tool track, 8 pp).  Build: lualatex + biber.
%    latexmk -lualatex pp2lp.tex      (or  make)
% ════════════════════════════════════════════════════════════════════
\documentclass[EPiC]{easychair}

\input{preamble.tex}    % fonts, listings/glyph map, tcolorbox, biblatex
\input{macros.tex}      % notation + system names (\pp, \lp, \tool, \rn, …)
\input{results-data.tex}% ALL benchmark numbers live here — edit only this

\title{Independently Checking Atelier~B's\\ Predicate Prover in LambdaPi}
\titlerunning{Independently Checking Atelier B's Predicate Prover}

% TODO(authors): confirm affiliations/emails before submission.
\author{
  Ciar\'an Dunne\inst{1}
  \and
  Guillaume Burel\inst{2}
}
\authorrunning{C. Dunne and G. Burel}
\institute{
  SAMOVAR, T\'el\'ecom SudParis, Institut Polytechnique de Paris,
  Palaiseau, France\\
  \email{ciaran-matthew.dunne@telecom-sudparis.eu}
  \and
  SAMOVAR, ENSIIE, Institut Polytechnique de Paris, \'Evry, France\\
  \email{guillaume.burel@ensiie.fr}
}

\begin{document}
\maketitle

\begin{abstract}
	\noindent
	Within the \bmethod{} ecosystem, the \emph{Predicate Prover} (\pp)
	is an automated theorem prover that discharges first-order
	proof obligations.
	%
	Because \pp's source code is not publicly available,
	its results are currently trusted without independent verification.
	%
	We present \tool, a tool that reconstructs \pp{} proofs in \lp,
	a proof assistant based on the \lpcmr.
	%
	Each of \pp's inference rules is encoded as an \lp{} symbol whose type
	captures the rule's premises and conclusion and whose body \emph{proves}
	the rule sound with respect to classical logic.
	%
	\pp{} can be instrumented to emit \emph{replays} of its proofs;
	\tool{} parses the replay, rebuilds the proof tree,
	and emits a tactic script that \lp{} independently type-checks.
	%
	We prove the soundness of nearly all of \pp's inference rules
	and reconstruct proofs across several (real-world and synthetic)
	benchmark suites.
\end{abstract}

% Sections (one \section each). Status is in each file's header comment:
%   intro, encoding = full drafts;  background, replay, eval, conclusion = skeletons.
\input{intro.tex}
\input{background.tex}
\input{encoding.tex}
\input{replay.tex}
\input{eval.tex}
\input{conclusion.tex}

\printbibliography

\end{document}
