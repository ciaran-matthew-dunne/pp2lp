% ── appendix.tex ──────────────────────────────
\appendix

\section{Typing Rules for \lp}\label{app:typing}

A signature $\Sigma$ declares typed constants and rewrite rules, and a context
$\Gamma$ is a list of typed variables.  The conversion relation $\Equiv$ is the
least congruence containing $\beta$-reduction and the rewrite rules of
$\Sigma$.  The typing judgement $\Gamma \vdash t : A$ is generated by the rules
below, with $s$ ranging over the sorts $\TYPE$ and $\KIND$ (we elide the
structural rules; see~\cite{assaf2023dedukti} for the full system):
\begin{center}
  \begin{prooftree}
    \hypo{}\infer1[\textsc{sort}]{\vdash \TYPE : \KIND}
  \end{prooftree}
  \qquad
  \begin{prooftree}
    \hypo{\Gamma \vdash A : s}\infer1[\textsc{var}]{\Gamma, x{:}A \vdash x : A}
  \end{prooftree}
  \qquad
  \begin{prooftree}
    \hypo{c : A \in \Sigma}\infer1[\textsc{const}]{\Gamma \vdash c : A}
  \end{prooftree}
\end{center}
\begin{center}
  \begin{prooftree}
    \hypo{\Gamma \vdash A : \TYPE}\hypo{\Gamma, x{:}A \vdash B : s}
    \infer2[\textsc{prod}]{\Gamma \vdash \Pi (x{:}A),\, B : s}
  \end{prooftree}
  \qquad
  \begin{prooftree}
    \hypo{\Gamma, x{:}A \vdash t : B}\hypo{\Gamma \vdash \Pi (x{:}A),\, B : s}
    \infer2[\textsc{abs}]{\Gamma \vdash \lambda (x{:}A),\, t : \Pi (x{:}A),\, B}
  \end{prooftree}
\end{center}
\begin{center}
  \begin{prooftree}
    \hypo{\Gamma \vdash t : \Pi (x{:}A),\, B}\hypo{\Gamma \vdash u : A}
    \infer2[\textsc{app}]{\Gamma \vdash t\,u : B\subst{x}{u}}
  \end{prooftree}
  \qquad
  \begin{prooftree}
    \hypo{\Gamma \vdash t : A}\hypo{\Gamma \vdash B : s}\hypo{A \Equiv B}
    \infer3[\textsc{conv}]{\Gamma \vdash t : B}
  \end{prooftree}
\end{center}
\noindent The \textsc{conv} rule is the one the encoding relies on throughout:
it silently applies the rewrite rules of \autoref{sec:enc-formulas} during
type-checking.


\section{LambdaPi Syntax}\label{app:syntax}

We reproduce the fragment of \lp's concrete syntax~\cite{hondet:hal-02981561}
used to write the developments in this paper, eliding only the interactive
query and search commands.  Identifiers $x$ may be qualified; $n$ and $s$
range over integer and string literals.  In the meta-notation, ${(\cdots)}^{?}$
marks an optional item and ${(\cdots)}^{*}$, ${(\cdots)}^{+}$ repetition;
all other brackets and punctuation are concrete syntax.

\paragraph{Terms and parameters.}
\[
\begin{array}{rcll}
  t, u & \Coloneqq & a\;{(→ t)}^{?} \;\mid\; a^{?}\,\mathit{bt} & \text{term} \\
  a    & \Coloneqq & h\;\mathit{arg}^{*} & \text{application} \\
  \mathit{bt} & \Coloneqq & \mathit{bd}\;\mathit{abs} \;\mid\; \kw{let}\;x\;p^{*}\,{(:\,t)}^{?}\,{≔}\;t\;\kw{in}\;t & \text{binder term} \\
  \mathit{bd} & \Coloneqq & λ \;\mid\; Π \;\mid\; \text{\textasciigrave}\,@^{?}\,x & \text{binder} \\
  h    & \Coloneqq & @^{?}\,x \;\mid\; \_ \;\mid\; \TYPE \;\mid\; (t) \;\mid\; n \;\mid\; s \;\mid\; \$x\,\mathit{env}^{?} \;\mid\; \mathord{?}x\,\mathit{env}^{?} & \text{head} \\
  \mathit{arg} & \Coloneqq & h \;\mid\; [\,t\,] & \text{argument} \\
  \mathit{abs} & \Coloneqq & p^{+}\,,\,t \;\mid\; p :\, t\,,\,t & \text{abstraction} \\
  \mathit{env} & \Coloneqq & .\,[\;{(t\;{(,\,t)}^{*})}^{?}\;] & \text{metavariable env.} \\
  p    & \Coloneqq & (x^{+} :\, t) \;\mid\; [\,x^{+}\,{(:\,t)}^{?}\,] \;\mid\; x \;\mid\; \_ & \text{parameter} \\
\end{array}
\]

\paragraph{Commands.}  Each command is terminated by a semicolon.
\[
\begin{array}{rcll}
  \mathit{cmd} & \Coloneqq & \mathit{exp}^{?}\,m^{*}\;\kw{symbol}\;x\;p^{*} :\, t\;{(\mathit{pf} \mid {≔}\;\mathit{tp})}^{?} & \text{declaration} \\
       & \mid & \mathit{exp}^{?}\,m^{*}\;\kw{symbol}\;x\;p^{*}\,{≔}\;t\;\mathit{pf}^{?} & \\
       & \mid & \mathit{exp}^{?}\,p^{*}\;\kw{inductive}\;\mathit{ind}\;{(\kw{with}\;\mathit{ind})}^{*} & \text{inductive} \\
       & \mid & \kw{rule}\;r\;{(\kw{with}\;r)}^{*} \;\mid\; \kw{coerce\_rule}\;r & \text{rewriting} \\
       & \mid & \kw{notation}\;x\;\mathit{nota} \;\mid\; \kw{opaque}\;x & \\
       & \mid & \kw{require}\;{({(\kw{private})}^{?}\,\kw{open})}^{?}\;x^{+} & \text{import} \\
       & \mid & {(\kw{private})}^{?}\,\kw{open}\;x^{+} & \\
  \mathit{exp} & \Coloneqq & \kw{private} \;\mid\; \kw{protected} & \text{exposition} \\
  m    & \Coloneqq & \mathit{side}^{?}\,\md{associative} \;\mid\; \md{commutative} \;\mid\; \md{constant} & \text{modifier} \\
       & \mid & \md{injective} \;\mid\; \md{opaque} \;\mid\; \md{sequential} & \\
  \mathit{side} & \Coloneqq & \kw{left} \;\mid\; \kw{right} & \text{side} \\
  \mathit{ind} & \Coloneqq & x\;p^{*} :\, t\;{≔}\;{|}^{?}\,{(\mathit{con}\;{(|\,\mathit{con})}^{*})}^{?} & \text{inductive def.} \\
  \mathit{con} & \Coloneqq & x\;p^{*} :\, t & \text{constructor} \\
  r    & \Coloneqq & t\;↪\;t & \text{rewrite rule} \\
  \mathit{nota} & \Coloneqq & \kw{infix}\;\mathit{side}^{?}\,f \;\mid\; \kw{prefix}\;f \;\mid\; \kw{postfix}\;f \;\mid\; \kw{quantifier} & \text{notation} \\
\end{array}
\]

\paragraph{Proofs and tactics.}
\[
\begin{array}{rcll}
  \mathit{tp} & \Coloneqq & t \;\mid\; \mathit{pf} \;\mid\; t\;\mathit{pf} & \text{term-proof} \\
  \mathit{pf} & \Coloneqq & \kw{begin}\;(\mathit{sub}^{+} \mid \mathit{ss}^{?})\;\mathit{pe} & \text{proof} \\
  \mathit{sub} & \Coloneqq & \{\;\mathit{ss}^{?}\;\} & \text{subproof} \\
  \mathit{ss}  & \Coloneqq & \mathit{st}\;{;}^{?} \;\mid\; \mathit{st}\;;\;\mathit{ss} & \text{steps} \\
  \mathit{st}  & \Coloneqq & \mathit{tac}\;\mathit{sub}^{*} & \text{step} \\
  \mathit{pe}  & \Coloneqq & \kw{end} \;\mid\; \kw{abort} \;\mid\; \kw{admitted} & \text{closer} \\
  \mathit{tac} & \Coloneqq & \kw{admit} \;\mid\; \kw{apply}\;t \;\mid\; \kw{assume}\;p^{+} \;\mid\; \kw{change}\;t & \text{tactic} \\
       & \mid & \kw{fail} \;\mid\; \kw{generalize}\;x \;\mid\; \kw{have}\;x :\, t \;\mid\; \kw{induction} & \\
       & \mid & \kw{refine}\;t \;\mid\; \kw{reflexivity} \;\mid\; \kw{remove}\;x^{+} \;\mid\; \kw{symmetry} & \\
       & \mid & \kw{rewrite}\;\mathit{side}^{?}\,{(.\,[\,\mathit{rw}\,])}^{?}\,t \;\mid\; \kw{simplify}\;x^{?} & \\
       & \mid & \kw{set}\;x\,{≔}\;t \;\mid\; \kw{try}\;\mathit{tac} \;\mid\; \kw{repeat}\;\mathit{tac} \;\mid\; \kw{orelse}\;\mathit{tac}\;\mathit{tac} & \\
  \mathit{rw}  & \Coloneqq & t \;\mid\; \kw{in}\;t \;\mid\; \kw{in}\;x\;\kw{in}\;t \;\mid\; t\;\kw{in}\;t\,{(\kw{in}\;t)}^{?} \;\mid\; t\;\kw{as}\;x\;\kw{in}\;t & \text{rw.\ pattern} \\
\end{array}
\]

% \section{Surviving Set-theoretic Operators}\label{app:operators}

% The operators below are the set-theoretic constructs that the pre-processing of
% \autoref{sec:pp-syntax} does \emph{not} unfold to membership; the encoding
% declares each as an uninterpreted constant over the domain $\El\,\iota$, and
% \pp{} only copies, compares, or instantiates them.

% \begin{center}
%   \begin{tabular}{l l l}
%     \toprule
%     Operator              & B notation                 & Reading \\
%     \midrule
%     interval              & $a \mathbin{..} b$          & integer range \\
%     maplet                & $a \mapsto b$               & ordered pair \\
%     set extension         & $\{a_1, \ldots, a_k\}$      & finite set \\
%     union, intersection   & $S \cup T$, \ $S \cap T$    & \\
%     relational image      & $r[S]$                      & image of $S$ under $r$ \\
%     relational inverse    & $r^{-1}$                    & converse relation \\
%     domain restriction    & $S \triangleleft r$         & pairs of $r$ with source in $S$ \\
%     range restriction     & $r \triangleright T$        & pairs of $r$ with target in $T$ \\
%     \bottomrule
%   \end{tabular}
% \end{center}


% \section{A Worked Result Derivation}\label{app:resderiv}

% For the body $P_1 \land P_2 \land P_3$ of \autoref{sec:enc-rules}, with each
% conjunct kept at a \rn{STOP\_1} leaf, the result derivation is:
% \begin{center}\footnotesize
%   \ebproofset{separation=1em}%
%   \begin{prooftree}
%     \infer0[\rn{STOP\_1}]{(\seq P_3) \yields [P_3]}
%     \infer0[\rn{STOP\_1}]{(\seq P_2) \yields [P_2]}
%     \infer0[\rn{STOP\_1}]{(\seq P_1) \yields [P_1]}
%     \infer2[\rn{AND4\_1}]{(\seq P_1 \land P_2) \yields [P_2, P_1]}
%     \infer2[\rn{AND4\_1}]{(\seq P_1 \land P_2 \land P_3) \yields [P_3, P_2, P_1]}
%     \hypo{}
%     \ellipsis{}{\seq (\Dia x \cdot P_3 \land P_2 \land P_1) \Rightarrow G}
%     \infer2[\rn{ALL7}]{\seq (\forall x \cdot P_1 \land P_2 \land P_3) \Rightarrow G}
%   \end{prooftree}
% \end{center}
% The two nested \rn{AND4\_1} steps concatenate their children's lists into the
% single flat list $[P_3, P_2, P_1]$, which \rn{ALL7} conjoins; combining eagerly
% with $\land$ would instead mirror the proof's nesting as
% $P_3 \land (P_2 \land P_1)$, so the list defers and flattens the conjunction.
% %
% Reconstructed, the \rn{ALL7} step becomes the proof term
% \[
%   \rn{ALL7}\ (\lambda v,\ \rn{AND4\_1}\ \rn{STOP\_1}\ (\rn{AND4\_1}\ \rn{STOP\_1}\ \rn{STOP\_1}))\ \ldots
% \]
% of type $\Prf\,((\allI v,\ P_1 \land P_2 \land P_3) \Rightarrow G)$, whose
% continuation antecedent $\restm\,(\rho\,v)$ reduces to $\bwand$ of the flat list
% $∎ ∷ P_3 ∷ P_2 ∷ P_1$.



% \section{Reconstruction Output}\label{app:reconstruct}
