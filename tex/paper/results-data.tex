% ════════════════════════════════════════════════════════════════════
%  LIVE BENCHMARK NUMBERS — single source of truth for the paper.
%  Tool and paper evolve together; refresh these as the suites move.
%
%  Last refreshed: 2026-06-10  (pp2lp run <suite> -q on `claude-bench-suite`,
%  commit 8972c43).  ALL SUITES GREEN:
%    og            30 ✓ / 0 ✗
%    prv-no-arith  13 ✓ / 0 ✗
%    synth        107 ✓ / 0 ✗
%    prv           70 ✓ / 0 ✗    <- full Atelier B corpus, now 100%
%    nrm_test      42 ✓ / 0 ✗    (NRM-rule coverage suite)
%    gemini       422 ✓ / 0 ✗
%    claude      1140 ✓ / 0 ✗    (hand-authored stress/coverage suite)
%
%  !! eval.tex's PROSE still tells the May story (prv 23/70, "the gap
%  !! is ..."): the gap narrative no longer holds — prv is fully green
%  !! and the arithmetic solver terminals (ARITH, EGALITE) now emit
%  !! generated certificates.  Rewrite eval.tex before submission.
% ════════════════════════════════════════════════════════════════════

% ---- Rule coverage -------------------------------------------------
% rule_db.ml: 138 rule entries + 3 phantoms (FIN/STOP_NORM/NRM).
% "Not implemented" = loud-failure paths a trace would hit: the NRM27/28/30
% solver-substitution dispatch arms (only NRM29 is wired trust-free) and
% the chain-form AR7_1/AR8_1 (replay-format work; no producible trace).
\newcommand{\numRulesCovered}{138}   % distinct rule entries in rule_db.ml
\newcommand{\numRulesSpec}{140}      % rules in PP spec Annexe A (approx.)
\newcommand{\numRulesNotImpl}{5}     % NRM27/28/30 dispatch + AR7_1/AR8_1 chains
\newcommand{\numRulesFired}{105}     % rule_db names exercised by some replay

% ---- og suite ------------------------------------------------------
\newcommand{\ogPass}{30}
\newcommand{\ogTot}{30}

% ---- prv-no-arith (non-arithmetic Atelier B subset) ----------------
\newcommand{\pvnaPass}{13}
\newcommand{\pvnaTot}{13}

% ---- synth (targeted feature tests) --------------------------------
\newcommand{\synthPass}{107}
\newcommand{\synthTot}{107}

% ---- prv (full Atelier B PRV corpus) -------------------------------
\newcommand{\prvPass}{70}
\newcommand{\prvTot}{70}

% ---- nrm_test (NRM normalisation-rule coverage) --------------------
\newcommand{\nrmPass}{42}
\newcommand{\nrmTot}{42}

% ---- gemini (synthetic corpus) -------------------------------------
\newcommand{\geminiPass}{422}
\newcommand{\geminiTot}{422}

% ---- claude (hand-authored pipeline stress / coverage suite) -------
\newcommand{\claudePass}{1140}
\newcommand{\claudeTot}{1140}

% ---- aggregate ------------------------------------------------------
\newcommand{\allPass}{1824}          % sum of the seven suites above
\newcommand{\allTot}{1824}
