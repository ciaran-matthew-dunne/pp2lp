% ════════════════════════════════════════════════════════════════════
%  LIVE BENCHMARK NUMBERS — single source of truth for the paper.
%  Tool and paper evolve together; refresh these as the suites move.
%
%  Last refreshed: 2026-06-12 (bench/results/*.json + file counts on
%  `main`).  ALL SUITES GREEN; the corpus is trust-free (0 `trust`
%  tokens in any emitted proof).
%
%  Suite layout since 2026-06-12: the old synth/nrm_test/gemini suites
%  are retired INTO claude.  Three suites remain: og, prv, claude.
%
%  Column semantics:
%    Goals    = inputs to the upstream toolchain (.but files; og has
%               none — its 30 checked-in traces are the source).
%    Replays  = complete replays the PP/REPLAY toolchain produces
%               (the rest are dropped at gen time: PP failures or
%               documented REPLAY truncation/corruption).
%    Verified = replays pp2lp translates and lambdapi type-checks.
% ════════════════════════════════════════════════════════════════════

% ---- Rule coverage -------------------------------------------------
% rule_db.ml: 138 rule entries + 3 markers (FIN/STOP_NORM/NRM).
% The 9 trust-bodied regrouping chain lemmas were REMOVED 2026-06-12;
% the library now contains no postulated rule at all.
\newcommand{\numRulesCovered}{138}   % distinct rule entries in rule_db.ml
\newcommand{\numRulesSpec}{140}      % rules in PP spec Annexe A (approx.)
\newcommand{\numRulesFired}{105}     % rule_db names exercised by some replay

% ---- og (checked-in traces; the baseline) --------------------------
\newcommand{\ogReplayed}{30}
\newcommand{\ogPass}{30}

% ---- prv (Atelier B rule-validation corpus) ------------------------
\newcommand{\prvGoals}{123}
\newcommand{\prvReplayed}{70}
\newcommand{\prvPass}{70}

% ---- claude (synthetic stress / coverage suite) --------------------
\newcommand{\claudeGoals}{1284}
\newcommand{\claudeReplayed}{1207}
\newcommand{\claudePass}{1207}

% ---- aggregate ------------------------------------------------------
\newcommand{\allReplayed}{1307}      % 30 + 70 + 1207
\newcommand{\allPass}{1307}

% ---- apero probe (raw industrial POs; frontier, not a gate) ---------
% Numbers from the 2026-06-12 sampling study (eval.tex §Raw proof
% obligations).  A full-corpus gen pass has since materialised ~7.1k
% PP-proved benchmarks under lp/bench/apero; refresh these once a
% full-suite verification run is done.
\newcommand{\aperoMedianHyps}{1\,700}% median hypothesis count (approx.)
\newcommand{\aperoSampleTraces}{124} % traces PP produced on the sample
\newcommand{\aperoSampleLost}{86}    % of those, lost by REPLAY
\newcommand{\aperoSampleVerified}{14}% sample replays verified end-to-end

% ---- timing (cold-emit run, 2026-06-12: rm *.lp/*.lpo, run all) -----
% Machine: 13th-gen Intel i9-13950HX laptop (32 hw threads), 32 GiB.
% og 0.4s + prv 13.2s + claude 8.9s wall; summed engine emit ≈130s and
% lambdapi check ≈300s of CPU time across the pool.
\newcommand{\benchMachine}{a 32-thread Intel i9 laptop}
\newcommand{\benchWallAll}{under half a minute}
\newcommand{\benchCpuAll}{about five CPU-minutes}
