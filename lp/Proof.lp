// Proof.lp — not needed in shallow encoding.
// The deep encoding used Sqnt, ▷, Res, obtain for
// sequent manipulation. In the shallow encoding,
// sequents are just π (conj H) → π G (functions).
// Helper lemmas live in the rule files that need them.
