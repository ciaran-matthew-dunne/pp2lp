// Subst.lp — commented out for shallow encoding.
// Capture-avoiding substitution (psub, esub, vpsub, vesub)
// operated on custom inductive types Prd/Exp/Var.
// TODO: determine if substitution is needed in the
// shallow encoding for quantifier rules (ALL/XST).
